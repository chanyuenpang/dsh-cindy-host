{"schemaVersion":1,"entryType":"final_answer","turnId":"99","occurredAt":"2026-09-17T08:18:50.638Z","message":"任务 #2 证据：诊断与修复都落在真实数据上——claw 适配器（`@veewo/dsh-claw-kit/index.js`）每次结算都用 `session.append('todo/write', { todos })` 把计划同步进 DSH 原生 todo dock，**本会话日志里有 159 条 `todo/write` 快照**；而 DSH 把该事件声明为 log-only UI state，转录折叠按定义丢弃它，手机又没有投影通道（它只从 `TodoWrite` **工具行**渲染计划卡）——所以桌面有 dock、手机什么都没有。修复是 `src/dsh-message-fold.js` 新增 `entryForTodoWrite`：每条**非空**快照翻译成一行 `todo_write` 工具调用（`id=todo-<seq>`、时间取快照自身、`todos` 只带 `content/status`），持久读与实时路径共用同一翻译，空快照刻意跳过（那是清空 dock 的语义，「无卡」≠「空卡」）。实测 `tools/probe-todo-rows.mjs`：本会话 **159 条快照 → 156 行手机可见行**，行内是 `[completed]/[in_progress]/[pending]` 的真实计划条目；`npm test` **405/405**。"}
